Yes — you should now give Replit a second prompt focused on analytics correctness, aggregation control, pivot-table behavior, table/chart switching, and a final Power BI–style dashboard. Power BI supports configurable aggregations such as Sum, Average, Count, Min, Max, and “Do not summarize,” and PivotTables/PivotCharts are specifically meant to summarize and present data interactively, so those behaviors are the right benchmark for your app.

Full prompt
Build on the current app exactly as it is, without changing the existing design, layout, visual identity, navigation, spacing, typography, or overall user experience. Keep the current UI intact and only add the missing analytics features and logic so the app becomes a reliable CSV-to-insight analytics tool with pivot-style exploration and a final dashboard experience similar to Power BI.

The most important priority is accuracy of results. Right now the tool is making aggregation mistakes, especially using mean/average when it should use sum. Fix this at the modeling and query layer. The app must default to the correct aggregation based on business logic, and it must always let the user override the aggregation manually when needed, because BI tools support multiple aggregation types and “Do not summarize” behavior depending on the field and use case.

Main product goal
The app must let users upload multiple CSV files, infer and confirm relationships, build a correct business data model, explore data in pivot-table style, switch between table view and chart view, and generate a final dashboard that looks and behaves like a lightweight Power BI reporting experience. Pivot-style analysis is essential because PivotTables are designed to summarize, analyze, filter, and present data, and PivotCharts are the chart equivalent of the same summarized result.

Non-negotiable rule
Do not redesign the current app.
Do not replace the existing UI.
Do not change the app’s design language.
Add new behavior within the existing design system only.

Core analytics corrections
The app must fix aggregation logic so measures are not summarized incorrectly. Power BI and similar BI tools allow explicit aggregation choices such as Sum, Average, Count, Distinct Count, Min, Max, Median, and Do Not Summarize, so this app must expose that same control model to the user.

Aggregation rules to implement
For every field detected from uploaded CSV files, classify it into one of these categories:

Additive measures: revenue, sales amount, quantity, cost, profit, impressions, clicks, sessions, leads, conversions; default aggregation = SUM.

Semi-additive measures: balances, inventory snapshots, monthly ending values; default aggregation depends on time context and may require special handling.

Non-additive ratios: conversion rate, bounce rate, margin %, CTR, CPL, CAC/LTV ratio, average duration; default aggregation = do not blindly sum and do not average unless the business logic supports it.

Identifiers: transaction_id, customer_id, deal_id, invoice_id, order_id; default = Do Not Summarize.

Categorical dimensions: region, segment, product, source, channel, stage, sales_rep; use for grouping, rows, columns, slicers, legends.

Dates: support hierarchy and grouping by day, week, month, quarter, year.

Critical behavior
Default numeric business totals to SUM, not average.

Never use Average as an automatic default for measures like revenue, quantity, cost, profit, sessions, clicks, impressions, leads, conversions, or deal value.

If a field name suggests a rate or percentage, do not sum it.

If a field is an ID, never sum it.

Provide a visible aggregation selector next to every measure in analysis mode.

Let the user switch aggregation to:

Sum

Average

Count

Distinct Count

Min

Max

Median

Do Not Summarize

Safe defaults examples
Use these examples as product rules:

revenue → default SUM

profit → default SUM

cost → default SUM

quantity → default SUM

deal_value → default SUM

weighted_value → default SUM

sessions → default SUM

page_views → default SUM

clicks → default SUM

impressions → default SUM

leads → default SUM

conversions → default SUM

conversion_rate → default AVERAGE only if row-level rates are valid to average, otherwise recompute from numerator / denominator

bounce_rate → default AVERAGE with warning if needed

customer_id → Do Not Summarize

transaction_id → Do Not Summarize / Count / Distinct Count as an option

Important metric rule
Whenever possible, derived ratios should be recomputed from base components instead of averaging precomputed percentages. For example:

Conversion Rate = total conversions / total sessions

CTR = total clicks / total impressions

CPL = total spend / total leads

Margin % = total profit / total revenue

Do not simply average row-level percentages unless that is explicitly requested.

Pivot table experience
The app must support a pivot-style analysis view similar to Excel/BI pivot tables, where users can drag or assign fields into Rows, Columns, Values, and Filters. Pivot tables are used to summarize, analyze, and present data, and users should be able to filter for ranges, values, top 10, bottom 10, and date periods.

Pivot features required
Implement a pivot analysis module with:

Rows area

Columns area

Values area

Filters area

Sort ascending / descending

Top N / Bottom N

Date grouping: day / week / month / quarter / year

Drill-down and drill-up

Expand / collapse grouped values

Subtotals and grand totals

Null handling controls

Formatting for numeric, currency, percentage, decimal, whole number

Pivot value behavior
A field in the Values area must always show its aggregation type clearly.

Users must be able to change aggregation from a dropdown.

The app must preview how the result changes when aggregation changes.

Grand totals must respect the selected aggregation logic.

Distinct Count must be supported for IDs and categories where useful.

Pivot result presentation
Allow users to show pivot output in two modes:

Table style

Chart style

The same pivot result should drive both views so the numbers stay consistent.

Table view and chart view
The app must let the user choose how to present results:

table style,

chart style,

or both side by side.

This is essential because PivotTables summarize data in table form and PivotCharts visualize the same summarized result.

Table-style requirements
The table mode should support:

grouped rows and columns,

totals and subtotals,

pinned headers,

sorting,

filtering,

export,

conditional formatting,

drillthrough to detail rows,

pagination or virtualization for large data.

Chart-style requirements
The chart mode should auto-suggest a suitable chart depending on the selected fields and aggregation:

line chart for trends over time,

bar/column chart for comparisons,

stacked bar/column for segmented contribution,

area chart for trend volume,

pie/donut only for small part-to-whole cases,

scatter for relationship analysis,

KPI cards for summary measures,

funnel for pipeline stage analysis.

Visual sync rules
Table and chart must use the same filtered dataset and the same aggregation definition.

When a user changes aggregation from Sum to Average, both the table and chart must update together.

When the user drills in on the table, the chart should update too.

Clicking a chart element should optionally filter the table.

Final dashboard like Power BI
After files are uploaded, relationships are confirmed, and fields are modeled correctly, the app must be able to generate a final interactive dashboard similar to Power BI in concept. Power BI dashboards and style guides commonly define KPI placement, filter placement, visual standards, and reusable layouts, so this app should produce a clean business reporting layer with those principles in mind.

Final dashboard requirements
Create a final dashboard page that includes:

KPI cards at the top

interactive filters/slicers

date range selector

key charts

detailed tables

drill-down support

chart cross-filtering where possible

saved business measures

clear titles and metric definitions

export options

Dashboard sections
The final dashboard should include at minimum:

Executive summary

total revenue

total profit

total customers

total deals

total conversions

key growth indicators

Trend analysis

revenue over time

pipeline over time

traffic/conversions over time

month-over-month and year-over-year comparisons

Segmentation

by region

by product

by customer segment

by channel/source

by sales rep

Operational detail

top and bottom performers

detailed pivot table

exceptions or anomalies

drillthrough records

Marketing and web performance

sessions

conversions

campaign performance

conversion rate

CTR

CPL

Pipeline and sales

stages

weighted pipeline

expected close value

rep performance

sales cycle metrics

Data modeling and relationships
Keep all previous requirements about relationship inference and business logic confirmation.

Additional modeling rules for correctness
Before answering any question or building any dashboard visual, the app must:

determine the grain of each table,

determine whether the visual/question mixes detail and summary tables,

prevent invalid joins,

warn about many-to-many risks,

prevent duplicate counting,

explain when a monthly summary table should stay separate from transaction-level fact tables.

Example with uploaded files
Use this example model if these files are uploaded:

sales_transactions.csv

customers.csv

website_analytics.csv

sales_pipeline.csv

monthly_kpis.csv

marketing_campaigns.csv

Expected modeling logic:

customers is a dimension table.

sales_transactions is a fact table at transaction grain.

sales_pipeline is a fact table at deal grain.

website_analytics is a fact table at daily-source grain.

marketing_campaigns is a fact table at campaign grain.

monthly_kpis is an aggregated monthly summary table and should not be row-level joined to transactional tables.

Add a date dimension and connect it appropriately.

Use shared dimensions such as customer, product, region, source, channel, sales_rep when valid.

Question answering requirements
The chatbot must be able to answer all major analytics questions correctly after the model is built.

It must answer questions like:
What is total revenue for the selected period?

Show revenue by month, region, product, and sales rep.

Compare Q1 vs Q2 revenue.

What is weighted pipeline value by stage?

Which channel has the highest conversions?

Which campaigns have the best CPL and CTR?

Show top 10 products by profit.

Show bottom 10 regions by revenue.

What is the trend of sessions and conversions over time?

Which customers have the highest lifetime value?

What changed compared to last month?

Detect any unusual spike or drop in revenue or traffic.

It must also answer safely
If a question is risky or ambiguous, the app must respond like this:

explain which table(s) are being used,

explain which relationship(s) support the answer,

explain which aggregation is applied,

warn if there is a modeling limitation,

ask a clarifying question if needed.

Field configuration panel
Add a field settings/configuration layer so users can review and override defaults.

For each field, allow the user to configure:

field type,

role: dimension / measure / key / date,

default aggregation,

formatting,

business label,

description,

visibility,

sort by field,

date hierarchy behavior.

This is required because BI tools let users control summarization behavior rather than relying only on automatic inference.

Must-have UX additions
Without changing the current design language, add:

Upload files area

Relationship review panel

Model summary panel

Pivot builder

View toggle: Table / Chart / Both

Aggregation selector on measures

Dashboard builder/output page

Explanation panel for “how this number was calculated”

Validation and safeguards
Before finalizing any answer, pivot output, or dashboard visual, validate:

Is the selected aggregation correct?

Is the field additive or ratio-based?

Is the join path valid?

Are there duplicate rows after join?

Are grand totals correct?

Is the result using summary data and detail data incorrectly?

Is there a safer way to calculate the metric?

If validation fails, show a warning and suggest a correction.

Technical expectation
Implement a semantic query layer so the chatbot, pivot table, chart engine, and dashboard all use the same business model and aggregation rules. Do not let each visualization calculate independently in a way that can produce inconsistent results.

Use one shared logic layer for:

relationship resolution,

metric definitions,

aggregation defaults,

filters,

drilldowns,

totals,

chart/table synchronization.

Delivery expectation
Before coding:

summarize the implementation plan,

list the exact modules/components you will add,

explain how you will preserve the current app design unchanged,

explain how you will prevent SUM vs AVERAGE mistakes,

explain how pivot table, chart view, and dashboard view will stay consistent,

show the expected logic using the uploaded CSV example.

Final instruction to Replit
This app must behave like a true analytics assistant, not a generic CSV viewer. The user must be able to:

upload multiple CSVs,

confirm relationships,

model the data safely,

analyze data in pivot-table style,

choose Sum vs Average vs other aggregations,

switch between table and chart views,

and generate a final Power BI–style dashboard from the correctly modeled data.

Accuracy is more important than speed. If the model is uncertain, ask the user. If the aggregation is risky, warn the user. If the join is invalid, block it.

Extra line to paste at the end
You can add this final line too:

“Treat aggregation control as a first-class feature. Default additive business measures to SUM, never silently average totals, and always show the user how each metric is being calculated