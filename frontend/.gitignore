# Next.js build / cache artifacts — runtime state, never commit
/.next/
/out/

# Dependencies
/node_modules/

# Local env files
.env*.local

# TypeScript incremental build cache
tsconfig.tsbuildinfo
*.tsbuildinfo
